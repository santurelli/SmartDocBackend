# Local profile overrides
# Disable batch scheduling for local development to prevent unwanted background tasks
smartdoc.batch.scheduling.enabled=false

# Batch working directory locale
smartdoc.batch.workdir=C:/tmp/smartdoc/batch
